# Plaintext Founding Tester Hub source — NEVER commit (repo is public). Edit this,
# then rebuild the encrypted page with: node build-gate.mjs "<access-code>"
beta-hub.src.html

# (Retired) old gated homepage source — replaced by the public index.html. Kept
# ignored in case a stale copy lingers locally; safe to delete.
homepage.src.html

# NOTE: app screenshots (assets/app-*.png) are now PUBLIC marketing assets used
# on the homepage — they are committed on purpose, no longer git-ignored.

.DS_Store

# Plaintext site source. The new site is built from here and published as a
# single self-contained file, so while it is still behind the /preview/ gate
# this MUST stay out of the public repo. It gets committed at launch.
src/

# Original full-resolution images. Only the resized copies in assets/ ship.
assets/raw/

# Local-only build of the real site, for previewing before it ships.
.local/
